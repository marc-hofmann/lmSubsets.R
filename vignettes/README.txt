

Benchmark results are obtained by running the benchmark scripts
'bm-NN.R'.  The generated data is saved in 'bm-NN.RData'.  To
re-generate results from scratch, rename or remove '*.RData' files.

The benchmark scripts are executed automatically and the results
loaded when generating the vignette.
